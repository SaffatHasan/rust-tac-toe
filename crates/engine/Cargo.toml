[package]
name = "rust-tac-toe-engine"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"], optional = true }
serde_json = { version = "1.0", optional = true }

[features]
wasm = ["serde", "serde_json"]

[lib]
name = "rust_tac_toe_engine"
